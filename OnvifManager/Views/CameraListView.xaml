<UserControl x:Class="OnvifManager.Views.CameraListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="640" d:DesignWidth="280">

    <Grid Background="{StaticResource BgPanel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BgPanelHeader}"
                BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="НАЙДЕННЫЕ КАМЕРЫ"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="11" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <Border Grid.Column="1" Style="{StaticResource CountChip}">
                    <TextBlock Text="{Binding Cameras.Count}"
                               Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" BorderBrush="{StaticResource Border}"
                BorderThickness="0,0,0,1" Padding="10,8">
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Фильтр по имени / IP…">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None"
                                                     AlignmentX="Left" AlignmentY="Center">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Фильтр по имени / IP…"
                                                           Foreground="{StaticResource TextDim}"
                                                           Margin="10,0,0,0" FontSize="12"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding CamerasView}"
                 SelectedItem="{Binding SelectedCamera}"
                 ItemContainerStyle="{StaticResource CameraListItem}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Удалить из списка"
                              Command="{Binding PlacementTarget.DataContext.RemoveCameraCommand,
                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="12,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="8" Height="8"
                                 VerticalAlignment="Center"
                                 Margin="0,0,10,0"
                                 Fill="{Binding Status, Converter={StaticResource CameraStatusBrush}}"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}"
                                       FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding MetaLine}"
                                       FontFamily="{StaticResource MonoFont}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMuted}"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
