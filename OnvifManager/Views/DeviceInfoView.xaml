<UserControl x:Class="OnvifManager.Views.DeviceInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Device Information" Style="{StaticResource H1}"/>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource Muted}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Loading overlay -->
        <Border Grid.Row="1" Background="{StaticResource BgSurface}" CornerRadius="8"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading..." FontSize="16"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}">
            <StackPanel>
                <!-- Info card -->
                <Border Style="{StaticResource Card}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="32"/>
                            <RowDefinition Height="32"/>
                            <RowDefinition Height="32"/>
                            <RowDefinition Height="32"/>
                            <RowDefinition Height="32"/>
                            <RowDefinition Height="32"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Manufacturer" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Manufacturer}"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Model" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Model}"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Firmware" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding FirmwareVersion}"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Serial Number" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SerialNumber}"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Endpoint" Style="{StaticResource FormLabel}"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Endpoint}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Profiles section -->
                <TextBlock Text="Media Profiles" Style="{StaticResource H2}" Margin="0,12,0,8"/>
                <Border Style="{StaticResource Card}" Padding="16">
                    <ItemsControl ItemsSource="{Binding Profiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BgElevated}" CornerRadius="4"
                                        Padding="12,8" Margin="0,0,0,6">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock Style="{StaticResource Muted}" FontSize="11">
                                            <Run Text="Token: "/>
                                            <Run Text="{Binding Token}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <!-- Services section -->
                <TextBlock Text="ONVIF Services" Style="{StaticResource H2}" Margin="0,12,0,8"/>
                <Border Style="{StaticResource Card}" Padding="16">
                    <ItemsControl ItemsSource="{Binding Services}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BgElevated}" CornerRadius="4"
                                        Padding="12,8" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Uri}" Style="{StaticResource TextLink}"
                                                   FontSize="12" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Namespace}"
                                                   Style="{StaticResource Muted}" FontSize="10"
                                                   VerticalAlignment="Center" Margin="16,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
