<UserControl x:Class="OnvifManager.Views.DiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:OnvifManager.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800">

    <Grid Margin="8,8,8,4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar: scan + manual add -->
        <Border Grid.Row="0" Background="{StaticResource BgSurface}"
                CornerRadius="6" Padding="10" Margin="0,0,0,6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Scan controls -->
                <Grid Grid.Row="0" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Network:" Style="{StaticResource FormLabel}"
                               Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding NetworkInterfaces}"
                              SelectedItem="{Binding SelectedNetworkInterface}"
                              MaxDropDownHeight="240"/>

                    <Button Grid.Column="2" Content="Scan"
                            Command="{Binding ScanCommand}"
                            Style="{StaticResource AccentButton}"
                            Margin="8,0,0,0"
                            IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBool}}"/>

                    <Button Grid.Column="4" Content="Remove"
                            Command="{Binding RemoveCameraCommand}"
                            Style="{StaticResource DangerButton}"/>
                </Grid>

                <!-- Row 1: Manual add controls -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Manual add:" Style="{StaticResource FormLabel}"
                               Margin="0,0,8,0"/>

                    <TextBox Grid.Column="1" Text="{Binding ManualIp, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="IP Address"/>

                    <TextBlock Grid.Column="2" Text=":" Style="{StaticResource FormLabel}"
                               Margin="4,0"/>

                    <TextBox Grid.Column="3" Text="{Binding ManualPort, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Port"/>

                    <TextBlock Grid.Column="4" Text="User:" Style="{StaticResource FormLabel}"
                               Margin="12,0,8,0"/>

                    <TextBox Grid.Column="5" Text="{Binding ManualUsername, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Username"/>

                    <TextBlock Grid.Column="6" Text="Pass:" Style="{StaticResource FormLabel}"
                               Margin="12,0,8,0"/>

                    <PasswordBox Grid.Column="7"
                                 helpers:PasswordBoxHelper.BindPassword="True"
                                 helpers:PasswordBoxHelper.BoundPassword="{Binding ManualPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Password"/>

                    <Button Grid.Column="8" Content="Add"
                            Command="{Binding AddManualCommand}"
                            Style="{StaticResource SuccessButton}"
                            Margin="8,0,0,0"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Camera list -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Cameras}"
                  SelectedItem="{Binding SelectedCamera}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160"/>
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="130"/>
                <DataGridTextColumn Header="Endpoint" Binding="{Binding Endpoint}" Width="180"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="120"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusMessage}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
