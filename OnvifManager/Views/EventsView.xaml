<UserControl x:Class="OnvifManager.Views.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="340">

    <UserControl.Resources>
        <Style x:Key="ParamLabel" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}">
            <Setter Property="Width" Value="115"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14">
        <StackPanel>

            <!-- Motion -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="ДЕТЕКЦИЯ ДВИЖЕНИЯ" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Включить" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding MotionEnabled}"/>
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="115"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="34"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Чувств." Style="{StaticResource FormLabel}"/>
                    <Slider Grid.Column="1" Minimum="0" Maximum="100"
                            Value="{Binding MotionSensitivity}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="{Binding MotionSensitivity, StringFormat={}{0:F0}}"
                               FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <!-- Analytics -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="АНАЛИТИКА" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Пересеч. линии" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding LineCrossing}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Вторжение" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding Intrusion}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Оставл. предмет" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ObjectLeft}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Распозн. лиц" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding FaceRecognition}"/>
                </StackPanel>
            </StackPanel>

            <!-- Notifications -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="УВЕДОМЛЕНИЯ" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Email" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding EmailNotify}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Звук тревоги" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AlarmSound}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="FTP-загрузка" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding FtpUpload}"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Text="{Binding StatusText}"
                       Style="{StaticResource Muted}" FontSize="11"
                       Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
