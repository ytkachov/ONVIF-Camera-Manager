<UserControl x:Class="OnvifManager.Views.PtzView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="340">

    <UserControl.Resources>
        <Style x:Key="ParamLabel" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}">
            <Setter Property="Width" Value="115"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14">
        <StackPanel>

            <!-- Capabilities -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="ВОЗМОЖНОСТИ PTZ" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Поддержка" Style="{StaticResource ParamLabel}"/>
                    <TextBox Text="{Binding Capabilities}" IsReadOnly="True" Width="200"/>
                </StackPanel>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="115"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="34"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Скорость пан." Style="{StaticResource FormLabel}"/>
                    <Slider Grid.Column="1" Minimum="0" Maximum="100"
                            Value="{Binding PanSpeed}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="{Binding PanSpeed, StringFormat={}{0:F0}}"
                               FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="115"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="34"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Скорость накл." Style="{StaticResource FormLabel}"/>
                    <Slider Grid.Column="1" Minimum="0" Maximum="100"
                            Value="{Binding TiltSpeed}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="{Binding TiltSpeed, StringFormat={}{0:F0}}"
                               FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="115"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="34"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Скорость зума" Style="{StaticResource FormLabel}"/>
                    <Slider Grid.Column="1" Minimum="0" Maximum="100"
                            Value="{Binding ZoomSpeed}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="{Binding ZoomSpeed, StringFormat={}{0:F0}}"
                               FontFamily="Consolas" FontSize="11"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <!-- Presets -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="ПРЕСЕТЫ" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Preset 1" Style="{StaticResource ParamLabel}"/>
                    <TextBox Text="{Binding Preset1}" Width="195"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Preset 2" Style="{StaticResource ParamLabel}"/>
                    <TextBox Text="{Binding Preset2}" Width="195"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Preset 3" Style="{StaticResource ParamLabel}"/>
                    <TextBox Text="{Binding Preset3}" Width="195"/>
                </StackPanel>
            </StackPanel>

            <!-- Patrol -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock Text="ПАТРУЛИРОВАНИЕ" Style="{StaticResource SectionTitle}"/>
                <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Включено" Style="{StaticResource ParamLabel}"/>
                    <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding PatrolEnabled}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Задержка, с" Style="{StaticResource ParamLabel}"/>
                    <TextBox Text="{Binding PatrolDelay}" Width="80"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Text="{Binding StatusText}"
                       Style="{StaticResource Muted}" FontSize="11"
                       Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
